# Default Locust configuration for ShopStream load testing.
# Override any of these via CLI flags or environment variables.

locustfile = loadtests/locustfile.py
host = http://localhost:8000

# Web UI settings
web-host = 0.0.0.0
web-port = 8089

# Logging
loglevel = INFO
