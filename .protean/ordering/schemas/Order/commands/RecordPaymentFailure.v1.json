{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Record that payment failed, returning the order to Confirmed for retry.",
  "properties": {
    "order_id": {
      "minLength": 1,
      "type": "string"
    },
    "payment_id": {
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
    },
    "reason": {
      "maxLength": 500,
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "order_id",
    "payment_id",
    "reason"
  ],
  "title": "RecordPaymentFailure",
  "type": "object",
  "x-protean-aggregate": "ordering.order.order.Order",
  "x-protean-element-type": "command",
  "x-protean-fqn": "ordering.order.payment.RecordPaymentFailure",
  "x-protean-type": "Ordering.RecordPaymentFailure.v1",
  "x-protean-version": 1
}
